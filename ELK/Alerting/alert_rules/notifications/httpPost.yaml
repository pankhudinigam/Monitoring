alert:
- "post"
http_post_url: "http://{{host}}:{{port}}/{{path}}"
http_post_payload:
  {{arg_name}}: {{arg_value}} # Change based on variables referred
http_post_static_payload:
  apikey: abc123 # will go like this with every request
http_post_timeout: 30 # 10
http_post_all_values: True